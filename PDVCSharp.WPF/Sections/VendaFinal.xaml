<Window x:Class="PDVCSharp.WPF.Sections.VendaFinal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PDVCSharp.WPF.Sections"
        mc:Ignorable="d"
        Title="VendaFinal"
        Height="450"
        Width="800">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#444444" Height="30">
            <Grid>
                <TextBlock Text="CAIXA - 01"
                           Foreground="White"
                           FontSize="12"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center" />

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0,0,10,0">
                    <TextBlock Text="📶" Foreground="White" FontSize="10" Margin="5,0" />
                    <TextBlock x:Name="TxtHora"
                               Text="{Binding Now, Mode=OneWay, StringFormat={}{0:HH:mm}}"
                               Foreground="White"
                               FontSize="10" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid>
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Top"
                        HorizontalAlignment="Left"
                        Margin="30">
                <TextBlock Text="🛒" FontSize="24" Foreground="#2151A1" />
                <TextBlock Text="SUPERMARKET - Finalizar Venda"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="#2151A1"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0" />
            </StackPanel>
        </Grid>
        <Grid>
            
        </Grid>
    </DockPanel>
</Window>
