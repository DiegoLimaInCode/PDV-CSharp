using System.Windows;

namespace PDVCSharp.WPF.Sections
{
    // Tela de Fechamento de Caixa — exibe o resumo e permite fechar o caixa
    public partial class Fechamento : Window
    {
        public Fechamento()
        {
            InitializeComponent();
        }
    }
}
