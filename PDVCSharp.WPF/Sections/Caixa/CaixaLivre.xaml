<UserControl
    x:Class="PDVCSharp.WPF.Sections.Caixa.CaixaLivre"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PDVCSharp.WPF.Sections.Caixa"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Background="Red" Visibility="Collapsed" />

        <StackPanel VerticalAlignment="Center">
            <TextBlock
                Margin="0,0,0,10"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="50"
                FontWeight="SemiBold"
                Text="Caixa Livre" />

            <TextBlock
                Margin="0,0,0,20"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="22"
                Text="Passe no leitor ou digite o código do produto" />

            <Button
                Padding="20,10"
                HorizontalAlignment="Center"
                Click="Button_Click"
                FontSize="18">
                Iniciar venda
            </Button>
        </StackPanel>
    </Grid>

</UserControl>
